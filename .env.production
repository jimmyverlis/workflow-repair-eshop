# Production API
VITE_API_URL=https://erp.workflow.repair/api

# Platform domain (the shared hosting domain for slug-based store URLs)
VITE_PLATFORM_DOMAIN=store.workflow.repair

# Environment
VITE_ENVIRONMENT=production
